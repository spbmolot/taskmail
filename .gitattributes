# Исходники хранятся и выгружаются с LF: расширение читает Chrome, а не Windows,
# и незаметная замена переводов строк только зашумляет историю.
* text=auto eol=lf

# Иконки — бинарные, никаких преобразований.
*.png binary
